
<!DOCTYPE html>
<html lang="fa" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تولدت مبارک!</title>
    <!-- فونت زیبا -->
    <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: #ffe6f0;
            overflow-y: auto;
            /* allow scrolling */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            position: relative;
            user-select: none;
        }

        /* Falling hearts */
        .heart {
            position: absolute;
            top: -50px;
            font-size: 1.5rem;
            opacity: 0.8;
            animation: fall linear;
            pointer-events: auto;
        }

        @keyframes fall {
            to {
                transform: translateY(110vh);
            }
        }

        /* Centered sexy message box */
        .message-box {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) scale(0.5);
            background: linear-gradient(135deg, rgba(255, 153, 204, 0.6), rgba(255, 102, 153, 0.8));
            border: 2px solid #ff6699;
            border-radius: 20px;
            padding: 30px;
            width: 90%;
            max-width: 360px;
            max-height: 80vh;
            /* allow box to scroll if content is tall */
            overflow-y: auto;
            text-align: center;
            box-shadow: 0 0 20px rgba(255, 102, 153, 0.7);
            backdrop-filter: blur(8px);
            opacity: 0;
            animation: pop 0.8s ease-out forwards;
            pointer-events: auto;
        }

        @keyframes pop {
            to {
                transform: translate(-50%, -50%) scale(1);
                opacity: 1;
            }
        }

        .message-box p {
            font-family: 'Great Vibes', cursive;
            font-size: 2rem;
            color: #d6336c;
            line-height: 1.4;
            min-height: 5rem;
            direction: rtl;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.2);
        }

        /* Pulsate on hover */
        .message-box:hover {
            animation: pulse 1.2s ease-in-out infinite;
        }

        @keyframes pulse {

            0%,
            100% {
                transform: translate(-50%, -50%) scale(1);
            }

            50% {
                transform: translate(-50%, -50%) scale(1.05);
            }
        }
    </style>
</head>

<body>
    <div class="message-box">
        <p id="typed"></p>
    </div>
    <script>
        // Typing effect
        const message = `تولدت مبارک عشق دلم ❤️

عزیزترینم...
امروز تولد توئه، روزی که یه فرشته مثل تو به دنیا اومد، و فقط خدا می‌دونه چقدر دلم می‌خواست امروز پیشت باشم، دستاتو بگیرم و تو چشات نگاه کنم و بگم: «چقدر دوستت دارم...»

الان ازت دورم، ولی باور کن لحظه‌به‌لحظه تو فکرت هستم و دارم با همه وجودم تلاش می‌کنم که زودتر بهت برسم.
تو برام نه فقط یه عشق، بلکه نوری هستی که توی تاریکی راهو نشونم می‌ده...

دعا می‌کنم امسال، شروع یه زندگی قشنگ‌تر برات باشه.
تولدت مبارک نفسم...
منتظرم، با دلِ تنگ و قلبی پر از عشق...

دوستت دارم، همیشه ❤️`;
        const typedElem = document.getElementById('typed');
        let idx = 0;
        function type() {
            if (idx < message.length) {
                typedElem.textContent += message[idx++];
                setTimeout(type, 80);
            }
        }
        document.addEventListener('DOMContentLoaded', type);

        // Falling hearts
        function createHeart() {
            const heart = document.createElement('div');
            heart.className = 'heart';
            heart.textContent = '💖';
            heart.style.left = Math.random() * 100 + 'vw';
            const size = Math.random() * 1.2 + 0.8;
            heart.style.fontSize = size + 'rem';
            const duration = Math.random() * 3 + 5;
            heart.style.animationDuration = duration + 's';
            heart.style.animationTimingFunction = 'ease-out';
            document.body.appendChild(heart);
            setTimeout(() => heart.remove(), duration * 1000);
        }
        setInterval(createHeart, 250);
    </script>
</body>

</html>
